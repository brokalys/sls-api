#import "./mutation.graphql"
#import "./query.graphql"

schema {
  mutation: Mutation
  query: Query
}

enum Category {
  APARTMENT
  HOUSE
  LAND
}

enum Type {
  SELL
  RENT
}

enum PriceType {
  TOTAL
  SQM
}
